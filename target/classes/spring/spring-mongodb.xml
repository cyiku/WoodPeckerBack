<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                        http://www.springframework.org/schema/context
                        http://www.springframework.org/schema/context/spring-context-3.1.xsd
                        http://www.springframework.org/schema/data/mongo
                        http://www.springframework.org/schema/data/mongo/spring-mongo-1.0.xsd"
        >

    <!-- 自动扫描 -->
    <context:component-scan base-package="com.woodpecker.service" />

    <!-- 配置连接池 -->
    <mongo:mongo id="mongo" host="${mongo.host}" port="${mongo.port}">
        <mongo:options connections-per-host="${mongo.options.connections-per-host}"
                        threads-allowed-to-block-for-connection-multiplier="${mongo.options.threads-allowed-to-block-for-connection-multiplier}"
                        connect-timeout="${mongo.options.connect-timeout}"
                        max-wait-time="${mongo.options.max-wait-time}"
                        auto-connect-retry="${mongo.options.auto-connect-retry}"
                        socket-keep-alive="${mongo.options.socket-keep-alive}"
                        socket-timeout="${mongo.options.socket-timeout}"
                        slave-ok="${mongo.options.slave-ok}"
                        write-number="${mongo.options.write-number}"
                        write-timeout="${mongo.options.write-timeout}"
                        write-fsync="${mongo.options.write-fsync}"/>
    </mongo:mongo>

    <!-- mongo的工厂，通过它来取得mongo实例,dbname为mongodb的数据库名，没有的话会自动创建 -->
    <mongo:db-factory id="mongo_Weibo" dbname="${mongo.db.Weibo}" mongo-ref="mongo" />

    <!-- mongodb的主要操作对象，所有对mongodb的增删改查的操作都是通过它完成 -->
    <bean id="mongoWeibo" class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg name="mongoDbFactory" ref="mongo_Weibo"/>
    </bean>

    <!-- 添加第二个数据库，使用不同的id，并在MongoServiceImpl中使用时将变量命名为bean id-->
    <mongo:db-factory id="mongo_userCollection" dbname="${mongo.db.userCollection}" mongo-ref="mongo" />
    <bean id="mongoUserCollection" class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg name="mongoDbFactory" ref="mongo_userCollection"/>
    </bean>

</beans>